# XPowerSpirit-OpenWRT

Правила проекта (общие для всех агентов) — в **`AGENTS.md`** (корень репозитория).
Ключевое при работе с этим кодом:

1. Xray/nftables — только по официальной документации (`xray-config-links.md`), без «отсебятины».
2. Для структурных запросов по коду — Codebase Memory MCP
   (`list_projects` → `get_architecture` → `search_graph`/`trace_path` → `get_code_snippet`);
   конфиги/доки — обычным чтением.
